<h1><%= @question.tile %> </h1>

<%= link_to "Add Traits" , add_traits_path(params[:casestudy_id], @question) %>

<table>
  <tr>
    <th>ID</th>
    <th>Title</th>
  </tr>
       
  <% @traits.each do |t| %>
  <tr>
     <td>  <%= t.id %> </td>
     <td>   <%= t.name %></td>
  </td>
   <% end %>